/loop 5m What are you working on? What's left to do? Anything you need help with unblocking? Make sure that all blockers/deferred items are created as tracker items. keep running /cycle until the tracker backlog is empty (sprints.backlog == 0 and items.backlog == 0). If the backlog is empty, have the entire agent panel perform a full codebase review.
