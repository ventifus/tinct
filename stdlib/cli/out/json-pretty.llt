# JSON Pretty Formatter — uses the prelude's to-json function.
#
# Currently produces compact JSON (same as json.llt). Indented pretty-printing
# is tracked in the json-pretty-indent sprint.
#
# Returns a String (the formatted JSON output). No emit calls.

[call $to-json %]
