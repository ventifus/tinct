# Raw output formatter
# If % is a String, return it unquoted
# If % is a Seq, join elements with newlines
# Otherwise error

[if [str? %]
  %
  [if [seq? %]
    [join "\n" %]
    [error "raw formatter: expected String or Seq"]]]
