# NDJSON input formatter
# Reads %stdin line-by-line, parses each non-empty line as JSON,
# and produces a Seq of the parsed tinct values as %

[json: [include %libdir "codecs/json.llt"]]
[map [fn [let line] [json.from-json line]] [filter [fn [let line] [not [= "" line]]] [lines %stdin]]]
