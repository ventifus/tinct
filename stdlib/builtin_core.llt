# builtin_core.llt — Core types and fundamental primitives.
# This is the source of truth for all "core" module builtin type declarations.
# Every entry in core_builtins() in src/builtins_core.rs must be declared here.
# Bodies are Placeholder — valid tinct, errors if forced (builtins are Rust-side).

[
  # ── Opaque runtime types ──────────────────────────────────────────────────
  # Program: parsed+resolved AST from builtin-parse / builtin-resolve.
  Program:      [type Program]
  # TypeContext: accumulated type-checker state.
  TypeContext:  [type TypeContext]
  # File: raw OS file handle (Value::File).
  File:         [type File]

  # ── Rust-backed primitive types ───────────────────────────────────────────
  # Bootstrap fallback: these declarations allow builtin_core.llt to be type-checked
  # before prelude loads (when type-stage TypeNode values aren't yet available).
  # In user code (prelude loaded), the type-stage takes priority: Integer resolves
  # to TypeNode.Int → Type::Int, not TyCon("Integer"). See resolve_type_head ordering.
  Integer: [type Integer]
  String:  [type String]
  Float:   [type Float]
  # Bytes has no bare TypeNode in the type-stage (the type-stage Bytes is a function
  # for @[Bytes N] parameterized form). Bare @Bytes resolves through this declaration.
  Bytes:   [type Bytes]
  Never:   [type Never]
  Any:     [type Any]
  Unknown: [type Unknown]
  Proxy:   [type Proxy]
  Dict:    [type Dict]
  Expr:    [type Expr]

  # ── Capability types ──────────────────────────────────────────────────────
  DirCap:  [type DirCap]
  NetCap:  [type NetCap]
  ClockCap:[type ClockCap]
  Handle:  [type Handle]
  Url:     [type Url]

  # ── Opaque Rust handle types ──────────────────────────────────────────────
  BuilderHandle: [type BuilderHandle]
  Task:          [type [let a] Task]
  Channel:       [type [let a] Channel]
  Context:       [type Context]
  ReactiveCell:  [type [let a] ReactiveCell]
  QuicSession:   [type QuicSession]
  Http2Session:  [type Http2Session]
  Http3Session:  [type Http3Session]
  Timezone:      [type Timezone]
  Uri:           [type Uri]
  Urn:           [type Urn]
  Decimal:       [type Decimal]
  BigInt:        [type BigInt]

  # ── Control flow ──────────────────────────────────────────────────────────
  builtin-raise: [fn@Never  [let msg@String] ...]
  builtin-try:   [fn@[bind: [a]] [let f@[Fn@a []]] ...]

  # ── Arithmetic — monomorphic typed variants ───────────────────────────────
  # Each variant handles exactly one type combination. Cross-type arithmetic
  # (Integer+Float) is handled in tinct by explicit conversion + float variant.
  builtin-int-add:      [fn@Integer [let a@Integer b@Integer] ...]
  builtin-float-add:    [fn@Float   [let a@Float   b@Float  ] ...]
  builtin-int-sub:      [fn@Integer [let a@Integer b@Integer] ...]
  builtin-float-sub:    [fn@Float   [let a@Float   b@Float  ] ...]
  builtin-int-mul:      [fn@Integer [let a@Integer b@Integer] ...]
  builtin-float-mul:    [fn@Float   [let a@Float   b@Float  ] ...]
  builtin-int-to-float: [fn@Float   [let n@Integer] ...]

  # ── Comparison — monomorphic typed variants ───────────────────────────────
  # Rust returns 1 (true) or 0 (false) as Integer.
  builtin-lt:        [fn@Integer [let a@Integer b@Integer] ...]
  builtin-int-gt:    [fn@Integer [let a@Integer b@Integer] ...]
  builtin-float-gt:  [fn@Integer [let a@Float   b@Float  ] ...]
  builtin-str-gt:    [fn@Integer [let a@String  b@String ] ...]
  builtin-int-gte:   [fn@Integer [let a@Integer b@Integer] ...]
  builtin-float-gte: [fn@Integer [let a@Float   b@Float  ] ...]
  builtin-str-gte:   [fn@Integer [let a@String  b@String ] ...]
  builtin-eq-int:    [fn@Integer [let a@Integer b@Integer] ...]
  builtin-eq-string: [fn@Integer [let a@String  b@String]  ...]

  # ── Dict primitives ───────────────────────────────────────────────────────
  builtin-keys:              [fn@Dict    [let xs@Dict] ...]
  builtin-dict-length:       [fn@Integer [let xs@Dict] ...]
  builtin-str-length:        [fn@Integer [let s@String] ...]
  builtin-bytes-length:      [fn@Integer [let b@Bytes] ...]
  # builtin-merge: implemented in tinct prelude and loader/test-loader (using builder API).
  # builtin-append: implemented in tinct prelude (derived from builtin-merge).
  builtin-get:               [fn@Any     [let k@Any xs@Dict] ...]
  builtin-has-key?:          [fn@Integer [let k@Any xs@Dict] ...]
  builtin-get-by-field:      [fn@Any     [let field@String k@Any xs@Dict] ...]
  builtin-dict-has-nth?:     [fn@Integer [let d@Dict n@Integer] ...]
  builtin-dict-nth:          [fn@Any     [let d@Dict n@Integer] ...]
  builtin-dict-has-key-nth?: [fn@Integer [let d@Dict n@Integer] ...]
  builtin-dict-key-nth:      [fn@Any     [let d@Dict n@Integer] ...]
  builtin-dict-has-kv-nth?:  [fn@Integer [let d@Dict n@Integer] ...]
  builtin-dict-kv-nth:       [fn@Any     [let d@Dict n@Integer] ...]
  builtin-build-dict:        [fn@Dict    [let entries@Dict] ...]

  # ── Builder ops ───────────────────────────────────────────────────────────
  builtin-make-builder:      [fn@BuilderHandle [] ...]
  builtin-builder-set:       [fn@BuilderHandle [let builder@BuilderHandle value@Any key@Any] ...]
  builtin-builder-delete:    [fn@BuilderHandle [let builder@BuilderHandle key@Any] ...]
  builtin-builder-finish:    [fn@Dict    [let builder@BuilderHandle] ...]
  builtin-builder-snapshot:  [fn@Dict    [let builder@BuilderHandle] ...]
  builtin-builder-has?:      [fn@Integer [let builder@BuilderHandle key@Any] ...]
  builtin-builder-get:       [fn@Any     [let builder@BuilderHandle key@Any] ...]
  builtin-builder-get-or:    [fn@Any     [let builder@BuilderHandle default@Any key@Any] ...]

  # ── String primitives ─────────────────────────────────────────────────────
  builtin-str-bytes:     [fn@Bytes   [let s@String] ...]
  builtin-str-hash:      [fn@Integer [let s@String] ...]
  builtin-int->string:   [fn@String  [let n@Integer] ...]
  builtin-str-slice:     [fn@String  [let s@String start@Integer end@Integer] ...]
  builtin-bytes-str:     [fn@String  [let b@Bytes] ...]
  builtin-str-index-of:  [fn@Integer [let needle@String s@String] ...]
  builtin-string-concat: [fn@String  [let a@String b@String] ...]

  # ── Bytes ─────────────────────────────────────────────────────────────────
  builtin-bytes:        [fn@Bytes [] ...]
  builtin-bytes-concat: [fn@Bytes [let a@Bytes b@Bytes] ...]

  # ── Type introspection ─────────────────────────────────────────────────────
  # These accept any value — the Any is genuine (type dispatch happens in Rust).
  builtin-type-of:         [fn@String  [let x@Any] ...]
  builtin-check-type:      [fn@Any     [let type-name@String x@Any] ...]
  builtin-variant-payload: [fn@Any     [let variant@Any] ...]
  builtin-tag-of:          [fn@String  [let x@Any] ...]
  builtin-llt-repr:        [fn@String  [let x@Any] ...]

  # ── Capability ─────────────────────────────────────────────────────────────
  builtin-cap-env-has?:  [fn@Integer [let name@String env@Any] ...]

  # ── I/O ───────────────────────────────────────────────────────────────────
  builtin-narrow:        [fn@DirCap  [let cap@DirCap ...sub] ...]
  builtin-list-dir:      [fn@Dict    [let cap@DirCap path@String] ...]
  builtin-path-dir:      [fn@DirCap  [let path@String] ...]
  builtin-file-open:     [fn@File    [let cap@DirCap path@String mode@String] ...]
  builtin-file-read:     [fn@Bytes   [let file@File n@Integer] ...]
  builtin-write-stdout:  [fn@[bind: [h] return: h] [let s@String h] ...]
  builtin-write-stderr:  [fn@[bind: [h] return: h] [let s@String h] ...]

  # ── Pipeline primitives ────────────────────────────────────────────────────
  builtin-parse:         [fn@Program [let src@Bytes path@String] ...]
  builtin-resolve:       [fn@Dict    [let prog@Any ...opts] ...]
  builtin-typecheck:     [fn@Program [let prog@Any ctx@TypeContext ...opts] ...]
  builtin-eval:          [fn@Dict    [let prog@Any ...opts] ...]

  # ── Module system ─────────────────────────────────────────────────────────
  builtin-module:        [fn@Dict    [let name@String] ...]
  builtin-extend-env:    [fn@Any     [let parent@Any bindings@Dict] ...]

  # ── TypeContext bootstrap ──────────────────────────────────────────────────
  builtin-get-type-context:       [fn@TypeContext [] ...]
  builtin-make-type-ctx:          [fn@TypeContext [] ...]
  builtin-tc-with-type-stage-env: [fn@TypeContext [let ctx@TypeContext env@Any] ...]

  # ── Sequences (core collection ops) ───────────────────────────────────────
  builtin-take:    [fn@Dict [let n@Integer xs@Dict] ...]
  builtin-drop:    [fn@Dict [let n@Integer xs@Dict] ...]
  builtin-concat:  [fn@Any  [let a@Any b@Any] ...]
  # builtin-dict-first, builtin-dict-last, builtin-str-first, builtin-str-last:
  # Implemented in tinct prelude (derived from builtin-dict-nth, builtin-str-slice).
  # builtin-rest, builtin-reverse, builtin-sort: implemented in tinct prelude.

  # ── Async ─────────────────────────────────────────────────────────────────
  builtin-channel: [fn@[Channel Any] [] ...]
  builtin-send:    [fn@Any [let ch@[Channel Any] val@Any] ...]
]
