# Space before dot prevents dot-access in new syntax.
# user.name (no space) = dot access on user
# [user "name"] (no dot at all) = call user with "name" arg
[user "field"]
=== out
[user "field"]

=== warn
[UndefinedVariable] undefined variable: user
