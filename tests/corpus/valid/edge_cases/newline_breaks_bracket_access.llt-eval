# Newline between identifier and bracket creates call form.
# a\n[0] (newline) = call a with arg [0] (a dict literal)
# Bracket access syntax has been removed; [a [0]] is always a call form.
[a
[0]]
=== out
[a [0]]

=== warn
[UndefinedVariable] undefined variable: a
