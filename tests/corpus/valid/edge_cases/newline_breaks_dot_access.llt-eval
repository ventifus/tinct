# Newline between identifier and dot: dot is always emitted as Dot token.
# a.b (no gap) = dot access; a\n.b = separate expressions (Call to a, then .b)
# Test: newline before dot creates separate expressions
[x: [a: 1]
x
.a]
=== out
["x": ["a": 1]  x.a]
