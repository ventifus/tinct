# Newline between identifier and dot: dot is always emitted as Dot token.
# Dot handler pops the preceding expression regardless of intervening newlines,
# so a.b and a\n.b are equivalent (both produce DotAccess).
[x: [a: 1]
x
.a]
=== out
["x": ["a": 1]  x.a]
