# Dot access: whitespace before '.' is allowed (same as Nix/Jsonnet).
# Both `config.host` and `config .host` parse as dot access.
# This test verifies the standard dot-access case.
[x: config.host]
===
["x": config.host]
