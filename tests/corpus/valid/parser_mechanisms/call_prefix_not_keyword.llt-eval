# "mycall" is an identifier in head position — triggers implied call.
# In new syntax, bare identifiers in head position are call targets.
# [mycall f x] is a call to mycall(f, x).
[mycall f x]
=== out
[mycall f x]
