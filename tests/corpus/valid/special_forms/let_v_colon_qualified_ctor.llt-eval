# [let v: Result.Ok] — REMOVED (T-1151).
# The name: Constructor structural test syntax inside [let ...] is now a parse error.
# The pending_rhs mechanism for assembling qualified dot-access RHS still applies
# but only for the default-value form (name: default_value).
[let v: Result.Ok]
=== out
<error at 4:6-4:18>
