# $_ in func position: parser produces a Call with $_ as func.
# Desugaring excludes func position from WRAP-CALL, so the Call
# is NOT wrapped in a lambda. This test verifies the parse-level AST
# (before desugaring) when $_ appears only in func position.
[_ x]
=== out
[_ x]

=== warn
undefined variable: _
