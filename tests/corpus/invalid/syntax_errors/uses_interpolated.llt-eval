# Interpolated string in uses: list is a parse error.
# Module names must be plain quoted strings like "core", not i"..." interpolated strings.
--- uses: [i"core"]
[]
=== out
2:12: interpolated strings not allowed in uses: list — use plain quoted strings like "core"
