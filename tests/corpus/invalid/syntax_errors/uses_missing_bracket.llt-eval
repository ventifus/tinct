# uses: without a bracket form is a parse error.
# The module list must be a bracket form: uses: ["core"], not uses: "core".
--- uses: "core"
[]
=== out
2:11: expected '[' after 'uses:' — module list must be a bracket form
