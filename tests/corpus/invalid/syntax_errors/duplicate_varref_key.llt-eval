# Duplicate key: same bare identifier appears twice as a dict key.
# Parse-time duplicate key detection covers bare identifier keys (normalized to Str before the check).
[k: a  k: b]
=== out
2:8: duplicate key "k"
