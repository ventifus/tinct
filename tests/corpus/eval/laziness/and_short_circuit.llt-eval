# Lazy evaluation: $and short-circuits when first argument is false
# If $and were eager, the $raise would fire. Since it's lazy, false is returned.
[and false [raise "should not evaluate"]]
=== out
Bool(false)
