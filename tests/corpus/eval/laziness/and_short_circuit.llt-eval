# Lazy evaluation: $and short-circuits when first argument is false
# If $and were eager, the $error would fire. Since it's lazy, false is returned.
[and false [error "should not evaluate"]]
===
Bool(false)
