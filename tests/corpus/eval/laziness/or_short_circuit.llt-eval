# Lazy evaluation: $or short-circuits when first argument is true
# If $or were eager, the $raise would fire. Since it's lazy, true is returned.
[or true [raise "should not evaluate"]]
=== out
Bool(true)
