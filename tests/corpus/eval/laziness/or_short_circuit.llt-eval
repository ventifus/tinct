# Lazy evaluation: $or short-circuits when first argument is true
# If $or were eager, the $error would fire. Since it's lazy, true is returned.
[or true [error "should not evaluate"]]
===
Bool(true)
