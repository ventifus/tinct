# Calling a function whose body accesses a binding with an error forces
# the error. The binding `x` is accessed in the return expression, so
# the error fires when `x` is forced.
[f: [fn [] [x: [raise "live"]] x]]
[f]
=== error
live
