# collect: materialize a Seq.Cons/Seq.Nil chain into a Dict
# Regression: collect must terminate on Seq.Nil (not empty Dict [])
[collect [range 1 5]]
=== out
Dict({0: Int(1), 1: Int(2), 2: Int(3), 3: Int(4)})
