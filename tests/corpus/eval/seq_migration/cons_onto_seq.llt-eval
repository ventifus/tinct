# cons: prepend onto a Seq delegates to $seq (O(1), lazy)
# Regression: cons must detect is_seq() and delegate to builtin_seq, not rebuild a Dict
[collect [cons 0 [range 1 4]]]
=== out
Dict({0: Int(0), 1: Int(1), 2: Int(2), 3: Int(3)})
