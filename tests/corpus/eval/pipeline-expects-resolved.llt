# Test that pipeline expects: annotations work with resolved types (not just string comparison)

--- expects: Int
42

=== out
Int(42)

---

# Test failure case
--- expects: Int
"hello"

=== error
type assert failed
