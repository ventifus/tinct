# Test: Pipeline input validation with record type annotation
# First document produces a typed record
[host: "localhost"  port: 3000]

--- %@[host: String  port: Int]
# Structural type check - should work with nominal matching for now

[url: [str "http://" %.host ":" [str %.port]]]

===
Dict({"url": String("http://localhost:3000")})
