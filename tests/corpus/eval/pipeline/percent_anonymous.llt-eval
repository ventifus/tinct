# Test bare % — anonymous pipeline variable as passthrough.
# The second document returns % directly (no field access), so the entire
# first document value is forwarded unchanged.
[x: 1]
---
%
===
Dict({"x": Int(1)})
