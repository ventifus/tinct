# Unannotated recursive function is valid after B-520.
# A function without a return type annotation that calls itself is accepted:
# Pass 1 pre-binds f as Type::Function; recursive calls return the pre-bound ret TypeVar.
[f: [fn [let x] [$f $x]]]
=== out
["f": [fn [let x] ...]]
