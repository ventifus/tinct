# Arithmetic with non-numeric argument: type checker rejects String operand
# The function is never called so eval succeeds; type checker catches the mismatch.
[f: [fn [x@String] [+ x 1]]]
=== out
Dict({"f": Function(x)})
=== warn
arithmetic operand has non-numeric type
