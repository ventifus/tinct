# Non-standard keys in fn@[...] metadata dict are stored in FnAnnotation.extra (no warning).
# Standard keys: return, constraint, doc, bind, kinds. Extra keys are passed through.
[fn@[return: Int  foo: "bar"] [] 42]
=== out
Function()
