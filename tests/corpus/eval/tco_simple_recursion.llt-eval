# Test simple tail recursion with 10,000 iterations.
# Pre-TCO this would hit continuation stack depth limit (~500-1000 iterations).
# With TCO, it runs in O(1) stack space.
[f: [fn [let n@Int] [if [= n 0] "done" [f [- n 1]]]]]
[f 10000]
=== out
String("done")
