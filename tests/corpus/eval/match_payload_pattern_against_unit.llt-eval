# [Tag n]: (binding pattern) against a unit variant should NOT match — falls through to next arm.
# D-1 equivalence applies only to [Tag]: (binding: None); [Tag n]: (binding: Some) requires a payload.
[
  [type Shape [Circle r: Int] [Square]]

  unit-shape: Square

  result: [match unit-shape
    [Square n]: "has-payload"
    [Square]:   "unit-match"]
]
=== out
Dict({"Shape": Variant(Shape, Null), "Circle": Function(payload), "Square": Variant(Square, Null), "unit-shape": Variant(Square, Null), "result": String("unit-match")})

=== warn
non-exhaustive match: missing coverage for Shape.Square at 7:11-9:30
