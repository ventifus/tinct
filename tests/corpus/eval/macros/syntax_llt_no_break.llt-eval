# Including syntax.llt should not break normal fn/class/type usage.
# All three keywords are handled by the parser directly; the macros only
# intercept programmatic Call nodes from other macros.
[include %libdir "syntax.llt"]
[f: [fn [let x y] [+ x y]]]
[f 10 20]
=== out
Int(30)
