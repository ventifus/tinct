# Task 2: Splice in expression position — now implemented (returns error).
# See macro_splice_expr_position.llt-eval for the real error test.
# This test keeps a simple non-splice sanity check.
[defmacro simple [args]
  [quote 42]]

[result: [simple]]
=== out
Dict({"result": Int(42)})
