# Test that [syntax-class: foo] is treated as a dict entry, NOT a syntax-class declaration.
# The colon-ahead guard should prevent special form recognition.
[syntax-class: "test"]
=== out
["syntax-class": "test"]
