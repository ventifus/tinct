# url is in "net" module; --- uses: ["net"] injects type sigs; [module "net"] for runtime access.
--- uses: ["net"]
[u: [call [get "url" [module "net"]] "https://example.com/api/v1"]]

u.port
=== out
Int(443)
