# cancel-task is lazy: the _: entry is never forced unless explicitly accessed.
# In lazy evaluation, [cancel-task ctx] is not materialized when only .result is accessed.
# cancelled? sees an uncancelled context and returns false.
[
  ctx: [context]
  _: [cancel-task ctx]
  result: [cancelled? ctx]
]
.result
=== out
Bool(false)
