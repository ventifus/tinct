# with-timeout (prelude wrapper) resolves to a Function value.
# The wrapper closes over %clock — calling it requires %clock in scope.
# This test only verifies the name is bound, not that it executes.
[type-of with-timeout]
=== out
String("Function")

=== warn
cannot unify ⊤ with <error> at 3:10-3:22
