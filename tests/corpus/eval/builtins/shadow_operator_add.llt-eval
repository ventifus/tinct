# Shadow + with a custom version that multiplies instead
# The real + would return 7 for (3 + 4), but our version returns 12
[
  +: [fn [let a b] [builtin-mul a b]]
  result: [+ 3 4]
]
=== out
Dict({"+": Function(a, b), "result": Int(12)})

=== warn
undefined variable: builtin-mul at 3:21-3:32
