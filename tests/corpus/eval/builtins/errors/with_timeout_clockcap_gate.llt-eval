# with-timeout: passing a non-ClockCap first argument produces a type error (E010).
# This verifies the ClockCap capability gate: only a real ClockCap value may be used.
# Passing 42 (Int) instead of a ClockCap triggers the runtime guard.
[with-timeout 42 [context] 1000]
=== error
[E010] type mismatch: expected ClockCap, got Int
