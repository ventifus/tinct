# with-deadline (prelude wrapper) resolves to a Function value.
# The wrapper closes over %clock — calling it requires %clock in scope.
# This test only verifies the name is bound, not that it executes.
[type-of with-deadline]
=== out
String("Function")

=== warn
[UnificationFailure] cannot unify Top with <error>
