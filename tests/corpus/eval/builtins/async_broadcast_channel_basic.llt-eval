# broadcast-channel arity error: no arguments
# Phase 3: broadcast-channel builtin (T-813)
#
# broadcast-channel is implemented; tests the arity validation path.
# Actual broadcast recv behavior requires multiple subscriber tasks and
# careful sequencing (each recv subscribes at call time, so receivers
# must be established before senders produce values). That coordination
# is not expressible as a simple corpus test.

[broadcast-channel]
=== error
[E080] broadcast-channel expects 1 argument, got 0
