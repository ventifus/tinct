# Test par-map: apply a function to each element concurrently
# par-map returns a Seq directly, not a Task, so no await needed

[
  double: [fn [let x] [* x 2]]
  nums: [range 1 6]
  result: [par-map double nums]
]
.result
=== out
Seq(Int(2), ...)
