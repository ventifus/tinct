# S-850 / B-338: TypeAssert pattern pipeline — binds scrutinee and uses it in body.
# T-1154: [@Int x] no longer binds x (x is Pattern::Pin — wildcard, no binding).
# Use a standalone [case [let x] x body] arm; x is bound to the scrutinee.
[match 42
  [case [let x] x [+ x 1]]]
=== out
Int(43)
