# GuardedValidate overlay-flatten error path: type assertion where the inner expression raises.
# Exercises eval_materialize.rs:1046-1050 — the flatten_overlay Err branch inside
# Cont::GuardedValidate. The [raise "error"] causes an eval error inside the merge expression,
# and the type assertion propagates that error correctly.
[@[x: Int] [merge [x: 1] [raise "not-a-dict"]]]
=== error
not-a-dict
