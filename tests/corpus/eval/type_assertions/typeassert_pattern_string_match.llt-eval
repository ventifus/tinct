# T-1154: [@String x] no longer binds x (x is Pattern::Pin — wildcard, no binding).
# Use a standalone [case [let x] x body] arm; x is bound to the scrutinee.
[match "hello"
  [case [let x] x x]]
=== out
String("hello")
