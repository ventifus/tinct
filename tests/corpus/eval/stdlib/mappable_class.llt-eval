# Mappable class declaration evaluates to an empty dict (marker value).
# The class registers "Mappable" in the runtime class registry.
[class [Mappable f]]
=== out
Dict({})

=== warn
[TypeError] class declaration must have a name declared with [class [ClassName ...] ...]
