# toml-lite: comments are ignored
[include %libdir "toml-lite.llt"]

---

[call %.parse-toml-lite "# comment\nkey = \"value\"\n# another comment"]
=== out
Variant(Result.Ok, Dict({"key": String("value")}))
