# values: large input (n=100) to verify O(n) performance (no timeout/stack overflow)
# Build a 100-element integer-keyed dict via collect+range, then extract values
[length [values [collect [range 0 100]]]]
=== out
Int(100)
