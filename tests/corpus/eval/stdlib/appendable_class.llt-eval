# Appendable class declaration evaluates to an empty dict (marker value).
# The class registers "Appendable" in the runtime class registry.
[class [Appendable a]]
=== out
Dict({})

=== warn
[TypeError] class declaration must have a name declared with [class [ClassName ...] ...]
