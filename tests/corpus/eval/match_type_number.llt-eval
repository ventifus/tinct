# Number: is a TypeAssertPending pattern — resolves to Type::Number (covers Int and Float).
# 3.14 is Float, which IS a Number, so the Number: arm matches.
[match 3.14
    Number:  "numeric"
    Str:     "text"
    _:       "other"]
=== out
String("numeric")
