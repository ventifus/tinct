# @Fn in TypeAssert position now resolves to Function{params:[], ret:Top, variadic:true}
# (the top of the function lattice). Non-callable values such as Int are rejected.
[@Fn 42]
=== error
[E011] type assertion failed
