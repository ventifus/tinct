# TypeAssert [@Int 42]: integer value satisfies Integer annotation.
# Corpus tests run via test-loader.llt (not as init program), so this exercises
# the type assertion after the full eval pipeline is initialized.
# The assertion passes because 42 is an Int, consistent with the @Int annotation.
[@Int 42]
=== out
Int(42)
