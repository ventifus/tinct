# builtin-type-of returns the value's type_val.
# During bootstrap, all values carry unknown_type_val() — an empty Dict.
# DisplayVisitor formats an empty Dict as Dict({}).
[builtin-type-of 42]
=== out
Dict({})
