# Null annotation: @Null resolves to Type::Record(Row::Empty).
# [@Null []] checks an empty dict against the closed empty-record type.
# Null is not a new type variant — it is an alias for the closed empty record.
[@Null []]
=== out
Dict({})
