# Guard pattern via case form — bind scrutinee to x, check guard inline.
# When guard fails (x <= 0), the else branch returns "no".
[match -1
  [case [let x] x [if [> x 0] "yes" "no"]]]
=== out
String("no")
