# Lazy evaluation: unused sequential dict bindings are never forced.
# A dead binding with an error does NOT fire — it is never accessed.
# The output is the final expression (42), not an error.
[bad: [raise "unused and not evaluated"]  good: 1]

42
=== out
Int(42)
