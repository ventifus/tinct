# TypeAssert: open record annotation still requires the declared fields to be present
# [@Any ...] wrapper bypasses inner typecheck so resolved_type is set on the outer TypeAssert.
# At runtime, validate_and_wrap_record rejects because "name" is missing (open records still
# require the declared fields; only extra fields are allowed).
[@[name: String  ...] [@Any [score: 42]]]
===
[E011] type assertion failed: expected record with field "name", got record missing field "name"
