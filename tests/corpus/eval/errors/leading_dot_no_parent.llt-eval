# Leading-dot (.name) with no parent-scope binding for 'b' produces an error
# at evaluation time. The resolver marks .b as unresolvable; the lowerer emits
# a LowerDiagnostic and CoreExpr::Placeholder; forcing the thunk propagates
# the diagnostic as a user error.
[a: .b]
=== error
undefined variable: .b
