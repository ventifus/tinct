# reduce auto-unpacks the payload of a Variant before iterating
# variant-payload creates a Variant whose payload is a Seq
# reduce should unpack the Seq and fold over its elements
[reduce [+ _ _] 0 [variant-payload "Wrapper" [1 2 3 4 5]]]
=== error
syntax error at 4:20 (cannot evaluate error node)
