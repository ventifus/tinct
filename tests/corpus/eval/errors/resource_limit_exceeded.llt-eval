# E043: ResourceLimitExceeded - currently not triggerable via corpus tests
# This error is reserved for future resource limits (e.g., collection size, string size)
# that are not yet implemented in the evaluator.
#
# COVERAGE STATUS: E043 has no corpus test because no resource limits are currently
# enforced beyond depth limits (which use E040). This placeholder documents the gap.
#
# Once resource limits are added (e.g., MAX_COLLECTION_SIZE, MAX_STRING_SIZE),
# this test should be updated to trigger E043.

# Placeholder: create a very large dict (this does NOT currently trigger E043)
[large: [range 100]]
===
Dict({"large": Seq(0..100)})
