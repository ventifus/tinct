# Forcing an iterate-based cyclic computation through collect should error
# iterate [fn [let x] x] produces an infinite seq of the seed value
# seed is [raise "cycle"] — forces when collected
[data: [iterate [fn [let x] x] [raise "cycle"]]
 result: [collect [take 5 data]]]
=== error
cycle
