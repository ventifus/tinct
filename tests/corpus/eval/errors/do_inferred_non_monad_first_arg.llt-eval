# Inferred [do] with non-constructor first binding — should fail
# The first binding's RHS is a plain variable (not a monad constructor or monad-typed value),
# so monad inference should fail with T_DO_INFER.
[do [x: some_var] [Ok x]]
=== error
[E002] undefined variable: ℊꜱʏᴍ⧼do-infer⧽2 (defined at 1:1-1:1) (called at 3:1-3:26)
  in [<dot-access> ...] at 3:1-3:26
  in expansion of `do` at 3:1

=== warn
cannot infer monad for [do] — add an explicit monad argument (e.g., [do result ...]) at 3:1-3:26
