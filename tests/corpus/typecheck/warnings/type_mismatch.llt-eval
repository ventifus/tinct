# Type mismatch warning: function with @String return annotation but @Integer param.
# The body $x has type Int (from the @Integer parameter annotation), but the @String
# return annotation declares the return type should be String. The type checker
# reports "cannot unify Int with String". Eval proceeds regardless (type errors are
# advisory) and returns the function definition without calling it.
[f: [fn@String [let x@Integer] x]]
=== out
Dict({"f": Function(x)})

=== warn
[UnificationFailure] cannot unify String with Int
