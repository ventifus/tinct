# Handle capability mismatch: @[Handle Writable] annotation on Handle[Readable] value.
# The parameter h is annotated @[Handle Readable], giving it type
# Handle[__cap_flag_readable: Record({})]. The body annotates h with @[Handle Writable],
# asserting type Handle[__cap_flag_writable: Record({})]. These two Handle capability
# rows are disjoint — the type checker reports a T003 "cannot unify Handle[...]" warning.
# Eval succeeds because the function is never called — the body is unevaluated (lazy).
[f: [fn [let h@[Handle Readable]] [@[Handle Writable] h]]]
=== out
Dict({"f": Function(h)})

=== warn
[UnificationFailure] cannot unify [__cap_flag_readable: []] with [__cap_flag_writable: []]
