# Function return type annotation: return value is checked
[f: [fn [let x] [@Int [+ x 1]]]
 result: [f 41]]
=== out
Dict({"f": Function(x), "result": Int(42)})

=== warn
argument to `+` has unconstrained type — Addable constraint will be silently dropped
inferred type is Unknown — consider adding a type annotation
