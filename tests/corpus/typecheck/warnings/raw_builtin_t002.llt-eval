# T002 warning: user code references a raw builtin-* name not exposed by prelude.
# `builtin-pow` is the internal Rust primitive registered under the builtin-* form.
# User code should call `pow` from stdlib/math.llt instead.
# The type checker emits a T002 TypeDiagnostic with a helpful hint.
# Eval succeeds because `builtin-pow` is still present in the runtime environment.
[builtin-pow 2.0 10.0]
=== out
Float(1024)
=== warn
raw builtin `builtin-pow` is not available in user code
