# Constraint violation warning: a function body applies $+ to a string argument.
# $+ resolves to the prelude's + function (Number -> Number -> Number).
# Passing "hello" (String) where Number is expected triggers a unification error.
# Eval proceeds — the function body is never invoked so no runtime error occurs.
[f: [fn [let x] [call $+ x "hello"]]]
=== out
Dict({"f": Function(x)})

=== warn
ambiguous type variable
