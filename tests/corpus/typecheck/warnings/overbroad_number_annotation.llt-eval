# Over-broad annotation: fn@Number when body infers Int
# The type checker should emit a T012 Info diagnostic suggesting @Integer instead
[f: [fn@Number [let x@Integer] x]]
=== out
Dict({"f": Function(x)})

=== warn
annotation @Number is over-broad — inferred type is Int; consider using @Integer
