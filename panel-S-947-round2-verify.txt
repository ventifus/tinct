S-947 Panel Round 1 — Fix Verification Report
===============================================
Verifier: panel round 2 (manual file-by-file check)
Sprint: S-947 (value-send-migration)
Source of findings: C-517 (tracker context note)
Fix implementation record: C-518

Legend:
  VERIFIED  — change is present and correct as described
  PARTIAL   — change is present but incomplete or has a residual issue
  DISPROVEN — change was not made or is incorrect

-----------------------------------------------------------------------

F-01 | arena.rs — stale panic claim in AnnotatedWrap comment
-----------------------------------------------------------------------
STATUS: VERIFIED

File checked: /var/home/adenton/Projects/tinct/src/arena.rs lines 530-536

The docstring for translate_unevaluated_state now reads:

  "AnnotatedWrap is handled by migrating its inner ThunkId and carrying ctx
  unchanged. ctx is Arc<EvalContext>, which keeps the source arena alive via the
  EvalContext's own Arc refcount — so carrying it into the migrated state does not
  produce a dangling reference. The Arc reference count holds the source arena open
  for exactly as long as any migrated AnnotatedWrap refers to it."

No mention of unreachable!() or any panic claim. The comment accurately describes
the correct behavior (migrates inner ThunkId, carries ctx safely).

-----------------------------------------------------------------------

F-02 | arena.rs — Dict branch missing cycle-break explanation
-----------------------------------------------------------------------
STATUS: VERIFIED

File checked: /var/home/adenton/Projects/tinct/src/arena.rs lines 436-448

The migrate_value Dict branch now has a detailed comment:

  "No pre-insert cycle-break sentinel is used here (unlike migrate_thunk_id's
  two-phase protocol for ThunkId-keyed thunks). This is correct: the cycle-break
  protocol uses thunk_map, which is keyed by ThunkId. Arc<Thunk> values stored
  directly in Value::Dict have no ThunkId identity — they are not registered in
  thunk_map and cannot be referenced by ThunkId from any UnevaluatedState field.
  Materialized Value::Dict cycles are also impossible in tinct's value model:
  a dict cannot contain itself as a value because Value is eagerly moved, not
  heap-aliased at the Value level."

The explanation is present and accurate.

-----------------------------------------------------------------------

F-03 | value.rs — DirCap Clone try_clone().expect() needs SAFETY comment
-----------------------------------------------------------------------
STATUS: VERIFIED

File checked: /var/home/adenton/Projects/tinct/src/value.rs lines 721-728

The DirCap arm in Clone impl now has:

  // SAFETY: DirCap values are always created from valid OS file descriptors
  // (main.rs and builtins_io.rs construction sites). try_clone() can fail with
  // EMFILE (too many open files) or EBADF (invalid descriptor) only if the
  // descriptor is closed or the fd table is exhausted. DirCap values are assumed
  // to remain valid for their Arc lifetime — the descriptor is never explicitly
  // closed while a DirCap holding it is alive.
  dir: dir.try_clone().expect("DirCap try_clone"),

The SAFETY comment is present and explains both failure modes (EMFILE, EBADF) and
the invariant that makes them unreachable.

-----------------------------------------------------------------------

F-04 | eval_dict.rs — dead empty_env + vestigial env param in tests
-----------------------------------------------------------------------
STATUS: VERIFIED

File checked: /var/home/adenton/Projects/tinct/src/eval_dict.rs

Grep for "empty_env" and "let _ = env": no matches found anywhere in eval_dict.rs.

The eval_str function (line 505) signature is:
  async fn eval_str(src: &str, ctx: &Arc<EvalContext>) -> EvalResult<Arc<Thunk>>

No env parameter. No `let _ = env;` line. The dead empty_env() function has been
deleted. The RwLock import has been removed from the test module.

-----------------------------------------------------------------------

F-05 | lower.rs — no Rust unit test for Decl-skip key contiguity
-----------------------------------------------------------------------
STATUS: VERIFIED

File checked: /var/home/adenton/Projects/tinct/src/lower.rs lines 2026-2072

Function test_builtin_lower_decl_skip_key_contiguity is present. It:
1. Constructs a SurfaceDocument with a leading SurfaceItem::Decl (TypeAlias)
   followed by SurfaceItem::Expr (Int literal).
2. Replicates the builtin_lower item-iteration loop with a separate expr_idx counter.
3. Asserts entries.len() == 1 (Decl is skipped).
4. Asserts entries[0].0 == "0" (first Expr gets key "0", not "1").

This test directly catches the enumerate() regression (where Decl would increment
the counter, giving the first Expr key "1" instead of "0").

-----------------------------------------------------------------------

F-06 | env.rs — merge_env_chain_into tests only tycon_defs, no slots test
-----------------------------------------------------------------------
STATUS: VERIFIED

File checked: /var/home/adenton/Projects/tinct/src/env.rs line 1047

Function test_merge_env_chain_slots_src_shadows_dst is present. It:
1. Creates dst with slot "foo" (name-only, scheme=None via insert_slot_name_only).
2. Creates src (child of dst) with slot "foo" that has a TypeScheme (Some).
3. Calls merge_env_chain_into(&src, &dst).
4. Asserts dst's "foo" slot now has scheme.is_some() (src shadows dst).

This tests the slots field specifically (the most semantically critical merge
field, per the finding) with later-document-wins semantics.

-----------------------------------------------------------------------

F-07 | ast.rs — no Span PartialEq/Hash unit tests after T-1771
-----------------------------------------------------------------------
STATUS: VERIFIED

File checked: /var/home/adenton/Projects/tinct/src/ast.rs lines 1441-1523

Three tests are present:

1. test_span_partialeq_excludes_name (line 1441):
   Verifies two Spans with same file/line/col but different name (foo vs bar)
   are equal. Uses assert_eq!.

2. test_span_hash_excludes_name (line 1467):
   Verifies two Spans at same file/line/col with different names produce
   identical hashes. Also checks name=Some("x") vs name=None hash equal.

3. test_span_partialeq_different_location_not_equal (line 1499):
   Verifies two Spans with different start_line are NOT equal even if name matches.

All three tests together pin the invariant: name is excluded from Span identity;
file/line/col are the identity fields.

-----------------------------------------------------------------------

F-08 | eval.rs — get_thunk_rc stale name
-----------------------------------------------------------------------
STATUS: VERIFIED

File checked: /var/home/adenton/Projects/tinct/src/eval.rs

Grep for "get_thunk_arc": 8 occurrences found:
  - Line 2141: function definition fn get_thunk_arc(thunk: &Arc<Thunk>) -> Arc<Thunk>
  - Lines 2310, 2367, 2955, 2984, 3380, 3485, 5140: call sites

Grep for "get_thunk_rc": 0 occurrences found.

The rename is complete. All 7 call sites use get_thunk_arc. The function body
(Arc::clone(thunk)) is correct for the post-T-1772 world where dict values are
Arc<Thunk> directly.

-----------------------------------------------------------------------

F-09 | surface_fmt.rs — misleading let _ = ctx; at line 59
-----------------------------------------------------------------------
STATUS: VERIFIED

File checked: /var/home/adenton/Projects/tinct/src/surface_fmt.rs lines 57-64

No `let _ = ctx;` line exists. The comment at line 63 reads:
  "// ctx is passed to to_tinct; only required if value is a Function."

This accurately describes the actual usage at line 64:
  out.push_str(&value.to_tinct(ctx)?);

The misleading "let _ = ctx;" suppressor and its stale comment have been removed.

-----------------------------------------------------------------------

F-10 | doc/internals/scope-arena.md — false "Code Issues" section
-----------------------------------------------------------------------
STATUS: VERIFIED

File checked: /var/home/adenton/Projects/tinct/doc/internals/scope-arena.md (full read)

Grep for "Code Issues", "slot_names", "lookup_name_in_scope_chain": none found in
the "Code Issues" pattern. The only match for "slot_names" is at line 23, where
the document correctly states: "There is no slot_names parallel vec on Scope."

The document accurately describes the actual code:
- Data model diagram: shows Scope with slots: Vec<Option<Arc<Thunk>>> and
  parent: Option<ScopeId>. No slot_names field.
- Invariants section (lines 85-91): states slot identity is positional, names
  carried by Thunk.span.name.
- API section: lists push_slot, reserve_slot, fill_slot, get_thunk,
  walk_parent_chain, collect_parent_chain, drop_scope. No lookup_name_in_scope_chain
  or iter_named listed.
- Comparison table (line 222): Name storage row correctly shows
  "Thunk.span.name (on each thunk)" for ScopeArena side.
- Arena Migration section (line 206): lists the 5 handled UnevaluatedState
  variants correctly (AstField, CoreExpr, BuiltinCall, FnCall, Guarded — no
  AnnotatedWrap listed, consistent with T-1770 deleting UnevaluatedState::Surface).

The Code Issues section that made false empirical claims has been deleted entirely.

-----------------------------------------------------------------------

F-11 | doc/16-architecture.md — BuiltinArgs wrong types
-----------------------------------------------------------------------
STATUS: PARTIAL

File checked: /var/home/adenton/Projects/tinct/doc/16-architecture.md

The primary fix target (line 47, the BuiltinFn signature description) is correct:
  "BuiltinArgs carries owned args: Vec<ThunkId>,
   named: Option<IndexMap<String, ThunkId>>, call_span: Span, ctx: Arc<EvalContext>"

This matches src/value.rs lines 50-51 exactly.

However, lines 348-349 (inside the "Sketch: Value Enum" section) still contain
the old incorrect types in a comment:

  // BuiltinArgs { args: Vec<Arc<Thunk>>, named: Option<IndexMap<String, Arc<Thunk>>>,
  //               call_span: Span, ctx: Arc<EvalContext> }
  // (updated for async — see src/value.rs for current type alias)

The Sketch section has a heading note "The authoritative implementation is in
src/value.rs ... which refine these types", and the comment itself ends with
"see src/value.rs for current type alias" — so the stale comment is somewhat
self-disclaiming. Nevertheless, it still shows the wrong types for BuiltinArgs
specifically, which is the exact issue F-11 was filed for.

The finding is PARTIALLY fixed: the prose description at line 47 is correct, but
the sketch comment at lines 348-349 was not updated and still shows Vec<Arc<Thunk>>
and Option<IndexMap<String, Arc<Thunk>>>.

-----------------------------------------------------------------------

SUMMARY
-----------------------------------------------------------------------

| #    | Finding                                         | Status   |
|------|-------------------------------------------------|----------|
| F-01 | arena.rs AnnotatedWrap comment (panic claim)    | VERIFIED |
| F-02 | arena.rs Dict branch cycle-break explanation    | VERIFIED |
| F-03 | value.rs DirCap Clone SAFETY comment            | VERIFIED |
| F-04 | eval_dict.rs empty_env + env param deleted      | VERIFIED |
| F-05 | lower.rs Decl-skip key contiguity unit test     | VERIFIED |
| F-06 | env.rs slots shadowing test added               | VERIFIED |
| F-07 | ast.rs Span PartialEq/Hash unit tests           | VERIFIED |
| F-08 | eval.rs get_thunk_rc → get_thunk_arc rename     | VERIFIED |
| F-09 | surface_fmt.rs let _ = ctx removed              | VERIFIED |
| F-10 | doc/internals/scope-arena.md Code Issues deleted| VERIFIED |
| F-11 | doc/16-architecture.md BuiltinArgs types        | PARTIAL  |

10 of 11 findings: VERIFIED
 1 of 11 findings: PARTIAL (F-11)

Residual for F-11:
  File: /var/home/adenton/Projects/tinct/doc/16-architecture.md
  Location: lines 348-349 (inside "Sketch: Value Enum" section)
  Issue: comment still shows Vec<Arc<Thunk>> and Option<IndexMap<String, Arc<Thunk>>>
  The comment ends with "see src/value.rs for current type alias" which mitigates
  the confusion, but the types shown are still wrong.
